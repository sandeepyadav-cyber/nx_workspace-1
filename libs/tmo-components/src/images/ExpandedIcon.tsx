import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const ExpandedIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="ExpandedIcon" {...props} />
);
export default ExpandedIcon;

import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const NewCrossIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="NewCrossIcon" {...props} />
);
export default NewCrossIcon;

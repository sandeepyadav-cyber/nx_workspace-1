import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const Markapproved: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="Markapproved" {...props} />
);
export default Markapproved;

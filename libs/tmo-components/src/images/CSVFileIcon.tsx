import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const CSVFileIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="CSVFileIcon" {...props} />
);
export default CSVFileIcon;

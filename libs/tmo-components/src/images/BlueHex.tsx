import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const BlueHex: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="BlueHex" {...props} />
);
export default BlueHex;

import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const RejectSnackbarIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="RejectSnackbarIcon" {...props} />
);
export default RejectSnackbarIcon;

import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const Dustbinicon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="Dustbinicon" {...props} />
);
export default Dustbinicon;

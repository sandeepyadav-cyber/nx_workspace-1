import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const SignOut: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="SignOut" {...props} />
);
export default SignOut;

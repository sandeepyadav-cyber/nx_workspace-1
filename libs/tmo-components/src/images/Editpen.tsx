import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const Editpen: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="Editpen" {...props} />
);
export default Editpen;

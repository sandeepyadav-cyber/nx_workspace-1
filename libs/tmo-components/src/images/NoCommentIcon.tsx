import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const NoCommentIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="NoCommentIcon" {...props} />
);
export default NoCommentIcon;

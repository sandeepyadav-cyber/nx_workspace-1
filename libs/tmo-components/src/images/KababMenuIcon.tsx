import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const KababMenuIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="KababMenuIcon" {...props} />
);
export default KababMenuIcon;

import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const BugIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="BugIcon" {...props} />
);
export default BugIcon;

import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const ExecutiveIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="ExecutiveIcon" {...props} />
);
export default ExecutiveIcon;

import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const InsightIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="InsightIcon" {...props} />
);
export default InsightIcon;

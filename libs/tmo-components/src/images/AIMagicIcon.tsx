import React from "react";
import { PlaceholderIcon } from "./_placeholderIcon";
import type { IconProps } from "./IconProps";

const AIMagicIcon: React.FC<IconProps> = (props) => (
  <PlaceholderIcon name="AIMagicIcon" {...props} />
);
export default AIMagicIcon;
